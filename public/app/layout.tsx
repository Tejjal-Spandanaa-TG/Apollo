import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Doctor Listing - Find the Best Doctors Near You",
  description:
    "Find and book appointments with the best doctors in your city. Filter by speciality, experience, and consultation fees.",
  keywords: "doctors, physician, medical, healthcare, appointment, consultation",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
